<Window x:Class="UbiGEM.Net.Simulator.Host.Popup.Settings.TriggerListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UbiGEM.Net.Simulator.Host.Popup"
        xmlns:Controls="clr-namespace:UbiGEM.Net.Simulator.Host.UserControls"
        mc:Ignorable="d"
        Title="Trigger List Editor" Height="500" Width="400" Loaded="Window_Loaded" WindowStartupLocation="CenterOwner" AllowsTransparency="True" WindowStyle="None" ResizeMode="CanResizeWithGrip">
    <Grid Background="{StaticResource UbiSamThemeBackground}">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>

        <Border BorderThickness="1" BorderBrush="{StaticResource UbiSamThemeBorderBrush}" Margin="5 5 5 0">
            <GroupBox Header="Trigger List Editor" >
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="32" />
                        <RowDefinition Height="7" />
                        <RowDefinition Height="32" />
                        <RowDefinition Height="7" />
                        <RowDefinition Height="32" />
                        <RowDefinition Height="7" />
                        <RowDefinition Height="32" />
                        <RowDefinition Height="7" />
                        <RowDefinition Height="32" />
                        <RowDefinition Height="7" />
                        <RowDefinition Height="1*" />
                        <RowDefinition Height="7" />
                        <RowDefinition Height="32" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="140" />
                        <ColumnDefinition Width="1*" />
                    </Grid.ColumnDefinitions>

                    <Label Grid.Column="0" Grid.Row="0" Content="Trigger ID" VerticalContentAlignment="Center" />
                    <Label Name="lblTriggerID" Grid.Column="1" Grid.Row="0" VerticalContentAlignment="Center" />

                    <Label Grid.Column="0" Grid.Row="2" Content="Trigger Mode" VerticalContentAlignment="Center" />
                    <Label Name="lblTriggerMode" Grid.Column="1" Grid.Row="2" VerticalContentAlignment="Center" />

                    <Label Grid.Column="0" Grid.Row="4" Content="Collection Event" VerticalContentAlignment="Center" />
                    <Label Name="lblCE" Grid.Column="1" Grid.Row="4" VerticalContentAlignment="Center" />

                    <Label Grid.Column="0" Grid.Row="6" Content="Report" VerticalContentAlignment="Center" />
                    <Label Name="lblReport" Grid.Column="1" Grid.Row="6" VerticalContentAlignment="Center" />

                    <Label Grid.Column="0" Grid.Row="8" Content="Variables" VerticalContentAlignment="Center" />
                    <DataGrid Name="dgrVariable" Grid.Column="1" Grid.Row="8" Grid.RowSpan="3" IsReadOnly="False" 
                            CanUserAddRows="False" CanUserDeleteRows="False" 
                            >
                        <DataGrid.Columns>
                            <DataGridComboBoxColumn Header="Variable" CanUserSort="False" Width="*" 
                                                    SelectedItemBinding="{Binding SelectedVariableInfo}"
                                                    DisplayMemberPath="DisplayStringForComboBox" 
                                                    ElementStyle="{StaticResource DataGridComboBoxColumnElement}"
                                                    >
                                <DataGridComboBoxColumn.EditingElementStyle>
                                    <Style TargetType="ComboBox" BasedOn="{StaticResource DataGridComboBoxColumnEditing}">
                                        <Setter Property="ItemsSource" Value="{Binding CandidateVariables,UpdateSourceTrigger=PropertyChanged}" />
                                        <EventSetter Event="SelectionChanged" Handler="dgcVariable_SelectionChanged" />
                                    </Style>
                                </DataGridComboBoxColumn.EditingElementStyle>
                            </DataGridComboBoxColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Label Grid.Column="0" Grid.Row="12" Content="Value" VerticalContentAlignment="Center" />
                    <TextBox Name="txtValue" Grid.Column="1" Grid.Row="12" VerticalContentAlignment="Center" />
                </Grid>
            </GroupBox>
        </Border>
        <Border Grid.Row="1" Style="{StaticResource PopupBottomArea}">
            <Grid >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="80" />
                    <ColumnDefinition Width="10" />
                    <ColumnDefinition Width="80" />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="1" Content="Save" Click="btnSave_Click" />
                <Button Grid.Column="3" Content="Close" Click="btnCancel_Click" IsCancel="True"/>
            </Grid>
        </Border>
    </Grid>
</Window>
