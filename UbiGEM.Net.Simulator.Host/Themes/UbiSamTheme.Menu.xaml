<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:UbiSam.Common.Themes.WPF.Themes" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/UbiSam.Common.Themes.WPF;component/Themes/UbiSamTheme.MenuItem.xaml"/>
    </ResourceDictionary.MergedDictionaries>
    
    <Style x:Key="UbiSamThemeMenu" TargetType="{x:Type Menu}">
        <Style.Resources>
            <Style x:Key="{x:Type MenuItem}" TargetType="{x:Type MenuItem}" BasedOn="{StaticResource UbiSamThemeMenuItem}"/>
        </Style.Resources>
        
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Background" Value="{DynamicResource UbiSamThemeBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource UbiSamThemeControlForeground}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource UbiSamThemeBorderBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Menu}">
                    <Border Grid.Row="0" Grid.RowSpan="4" 
                            Background="{TemplateBinding Background}"  
                            BorderThickness="{TemplateBinding BorderThickness}" 
                            BorderBrush="{TemplateBinding BorderBrush}"
                            Padding="{TemplateBinding Padding}"
                            Margin="{TemplateBinding Margin}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="10"/>
                                <RowDefinition Height="30"/>
                                <RowDefinition Height="8"/>
                                <RowDefinition Height="5"/>
                                <RowDefinition Height="4"/>
                                <RowDefinition Height="11"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="1" ClipToBounds="True" Orientation="Horizontal" IsItemsHost="True"/>

                            <Border Grid.Row="3" BorderThickness="0,1,0,1">
                                <Border.BorderBrush>
                                    <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                        <GradientStop Color="{DynamicResource UbiSamThemeGridSplitterUpBorderBrush}" Offset="0.0" />
                                        <GradientStop Color="{DynamicResource UbiSamThemeGridSplitterDownBorderBrush}" Offset="1.0" />
                                    </LinearGradientBrush>
                                </Border.BorderBrush>
                            </Border>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>