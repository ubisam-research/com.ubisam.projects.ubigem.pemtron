<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:wpf="clr-namespace:UbiSam.Common.Themes.WPF">
    <Style x:Key="UbiSamThemeHeaderedContentControl" TargetType="{x:Type HeaderedContentControl}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type HeaderedContentControl}">
                    <StackPanel>
                        <!--<:ColorZone>
                            <ContentPresenter ContentSource="Header"/>
                        </:ColorZone>-->
                        <ContentPresenter/>
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="UbiSamThemeGroupBox" TargetType="{x:Type GroupBox}">
        <Setter Property="wpf:GroupBoxAssist.HeaderColor" Value="{DynamicResource PrimaryHueDarkBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource UbiSamThemeBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1,0,1,1"/>
        <Setter Property="Padding" Value="10"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type GroupBox}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="37"/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Background="{TemplateBinding wpf:GroupBoxAssist.HeaderColor}" Padding="25,0,0,0">
                            <ContentPresenter ContentSource="Header" Width="Auto" Height="Auto"
                                              RecognizesAccessKey="True"
                                              VerticalAlignment="Center"
                                              TextBlock.FontSize="14"
                                              TextBlock.FontWeight="Bold"
                                              TextBlock.Foreground="{DynamicResource UbiSamThemeControlForeground}"/>
                        </Border>
                        <Border Grid.Row="1" Background="{DynamicResource UbiSamThemeBackground}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              TextBlock.FontSize="12"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>