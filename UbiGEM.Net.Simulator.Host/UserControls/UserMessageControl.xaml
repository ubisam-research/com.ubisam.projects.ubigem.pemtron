<UserControl x:Class="UbiGEM.Net.Simulator.Host.UserControls.UserMessageControl"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:UbiGEM.Net.Simulator.Host.Popup"
        xmlns:Controls="clr-namespace:UbiGEM.Net.Simulator.Host.UserControls"
        mc:Ignorable="d" d:DesignHeight="500" d:DesignWidth="300" 
        Loaded="UserControl_Loaded"
        >
    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="250" />
            <RowDefinition Height="19" MinHeight="19" MaxHeight="19" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        <Border Grid.Row="0" BorderThickness="1" BorderBrush="{StaticResource UbiSamThemeBorderBrush}" Margin="5 0 0 0" >
            <GroupBox Header="User Message List" >
                <Grid Margin="0 -25 0 0" >
                    <Grid.RowDefinitions >
                        <RowDefinition Height="24" />
                        <RowDefinition />
                    </Grid.RowDefinitions>

                    <Controls:ClientMenuControl x:Name="ctlFunctionUserMessage" Grid.Row="0" OnAdd="UserMessage_OnAdd" OnRemove="UserMessage_OnRemove" Margin="0 -27 1 0" />

                    <DataGrid Name="dgrUserMessages" Grid.Row="1" SelectionChanged="grdUserMessages_SelectionChanged" FrozenColumnCount="1"
                              CanUserAddRows="False" CanUserDeleteRows="False" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" IsReadOnly="True"
                                          >
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" EditingElementStyle="{StaticResource DataGridTextColumnEditing}" ElementStyle="{StaticResource DataGridTextColumnElement}" />
                            <DataGridTextColumn Header="D" Binding="{Binding DirectionString}" EditingElementStyle="{StaticResource DataGridTextColumnEditing}" ElementStyle="{StaticResource DataGridTextColumnElement}" />
                            <DataGridTextColumn Header="S" Binding="{Binding Stream}" EditingElementStyle="{StaticResource DataGridTextColumnEditing}" ElementStyle="{StaticResource DataGridTextColumnElement}" />
                            <DataGridTextColumn Header="F" Binding="{Binding Function}" EditingElementStyle="{StaticResource DataGridTextColumnEditing}" ElementStyle="{StaticResource DataGridTextColumnElement}" />
                            <DataGridTextColumn Header="W" Binding="{Binding WaitBit}" EditingElementStyle="{StaticResource DataGridTextColumnEditing}" ElementStyle="{StaticResource DataGridTextColumnElement}" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </GroupBox>
        </Border>

        <GridSplitter Grid.Row="1" VerticalAlignment="Center" ResizeDirection="Rows"/>

        <Border Grid.Row="2" BorderThickness="1" BorderBrush="{StaticResource UbiSamThemeBorderBrush}" Margin="5 0 0 0" >
            <GroupBox Header="Message Information" >
                <Grid >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="80" />
                        <ColumnDefinition />
                        <ColumnDefinition Width="80" />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="32" />
                        <RowDefinition Height="7" />
                        <RowDefinition Height="32" />
                        <RowDefinition Height="7" />
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <Label Grid.Column="0" Grid.Row="0" Content="Name" VerticalContentAlignment="Center" />
                    <TextBox Name="txtName" Grid.Column="1" Grid.Row="0" Grid.ColumnSpan="2" VerticalContentAlignment="Center" />
                    <Grid Grid.Column="3" >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80" />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <Label Content="WaitBit" ToolTip="WaitBit" VerticalContentAlignment="Center" />
                        <CheckBox Grid.Column="1" Name="chkWaitBit" VerticalAlignment="Center"/>
                    </Grid>
                    <Label Grid.Column="0" Grid.Row="2" Content="Stream" VerticalContentAlignment="Center" />
                    <TextBox Name="txtStream" Grid.Column="1" Grid.Row="2" VerticalContentAlignment="Center" />
                    <Label Grid.Column="2" Grid.Row="2" Content="Function" VerticalContentAlignment="Center" />
                    <TextBox Name="txtFunction" Grid.Column="3" Grid.Row="2" VerticalContentAlignment="Center" />
                    <TextBox Name="txtAbonormalMessage" Grid.Column="0" Grid.Row="4" Grid.ColumnSpan="4"  MaxLength="4194304"
                             VerticalContentAlignment="Top" TextWrapping="Wrap" AcceptsReturn="True"
                                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" 
                                 />
                </Grid>
            </GroupBox>
        </Border>
        <Border Grid.Column="0" Grid.Row="3" BorderThickness="0" Style="{StaticResource PopupBottomArea}">
            <Grid >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="80" />
                    <ColumnDefinition Width="10" />
                    <ColumnDefinition Width="80" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="1" Content="Check" Click="btnCheck_Click" />
                <Button Grid.Column="3" Content="Send" Click="btnSend_Click" />
            </Grid>
        </Border>
    </Grid>
</UserControl>